naftiko: 1.0.0-alpha2
info:
  label: Google Cloud Data Fusion API — Locations
  description: 'Google Cloud Data Fusion API — Locations. 1 operations. Lead operation: Google Cloud Data Fusion List available
    locations. Self-contained Naftiko capability covering one Google Cloud Data Fusion business surface.'
  tags:
  - Google Cloud Data Fusion
  - Locations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GOOGLE_CLOUD_DATA_FUSION_API_KEY: GOOGLE_CLOUD_DATA_FUSION_API_KEY
capability:
  consumes:
  - type: http
    namespace: google-cloud-data-fusion-locations
    baseUri: https://datafusion.googleapis.com/v1
    description: Google Cloud Data Fusion API — Locations business capability. Self-contained, no shared references.
    resources:
    - name: projects-projectId-locations
      path: /projects/{projectId}/locations
      operations:
      - name: listlocations
        method: GET
        description: Google Cloud Data Fusion List available locations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: projectId
          in: path
          type: string
          description: The project ID.
          required: true
        - name: pageSize
          in: query
          type: integer
          description: Maximum number of results to return.
        - name: pageToken
          in: query
          type: string
          description: Page token for pagination.
    authentication:
      type: bearer
      token: '{{env.GOOGLE_CLOUD_DATA_FUSION_API_KEY}}'
  exposes:
  - type: rest
    namespace: google-cloud-data-fusion-locations-rest
    port: 8080
    description: REST adapter for Google Cloud Data Fusion API — Locations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/projects/{projectid}/locations
      name: projects-projectid-locations
      description: REST surface for projects-projectId-locations.
      operations:
      - method: GET
        name: listlocations
        description: Google Cloud Data Fusion List available locations
        call: google-cloud-data-fusion-locations.listlocations
        with:
          projectId: rest.projectId
          pageSize: rest.pageSize
          pageToken: rest.pageToken
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: google-cloud-data-fusion-locations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Google Cloud Data Fusion API — Locations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: google-cloud-data-fusion-list
      description: Google Cloud Data Fusion List available locations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: google-cloud-data-fusion-locations.listlocations
      with:
        projectId: tools.projectId
        pageSize: tools.pageSize
        pageToken: tools.pageToken
      outputParameters:
      - type: object
        mapping: $.
